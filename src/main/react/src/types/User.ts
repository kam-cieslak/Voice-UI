export type UserType = {
    username: string,
    email: string
    role: string
}

export type TokenType = {
    token: string
}

export type GameScoreType = {
    score: number
}

export type LeaderboardType = {
    login: string,
    score: number
}