export type UserType = {
    username: string,
    email: string
    role: string
}

export type TokenType = {
    token: string
}

export type LeaderBoardType = {
    username: string,
    score: number
}